<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.starshop.mapper.CartMapper">

    <resultMap id="CartItemResultMap" type="com.starshop.pojo.entity.CartItem">
        <!-- 主键字段映射：id标签（MyBatis会做性能优化） -->
        <id property="id" column="id"/>
        <!-- 普通非主键字段映射：result标签 -->
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="specId" column="spec_id"/>
        <result property="quantity" column="quantity"/>
        <result property="price" column="price"/>
        <result property="stock" column="stock"/>
        <result property="productName" column="product_name"/>
        <result property="productImage" column="product_image"/>
        <result property="specText" column="spec_text"/>

        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getCartList" resultMap="CartItemResultMap">
        <include refid="selectCartItem_where_Sql"/>
    </select>


    <sql id="selectCartItem_where_Sql">
        select
        c.id as id,
        c.user_id,
        c.product_id,
        c.spec_id,
        c.quantity,
        p.price,
        p.stock,
        p.name as product_name,
        p.cover_image as product_image,
        ps.spec_text,
        c.create_time,
        c.update_time
        from cart c
        inner join product p on c.product_id = p.id
        inner join product_spec ps on p.id = ps.product_id and c.spec_id=ps.id
        where c.user_id = #{userId}  <!--用where过滤用户，确保只返回该用户的购物车数据，userId不会为null -->
    </sql>

</mapper>